# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
(Project pinned to SDK 54 for Expo Go compatibility — the user's phone runs Expo Go with SDK 54 support.)
